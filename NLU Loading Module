import time
import sys

# loading animation added for visual effect - not relevant to actual runtime of program
def loadingAnimation():
    for seconds in range(0, 3):
        print(f"Processing Data{'.' * (seconds + 1)}   ", end = " ")
        time.sleep(0.6)
